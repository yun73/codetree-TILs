#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.

    int a,b,c,d,e;
    cin >> a;
    cin >> b >> c >> d >> e;

    cout << (a > b) << endl;
    cout << (a > c) << endl;
    cout << (a > d) << endl;
    cout << (a > e) << endl;

    return 0;
}